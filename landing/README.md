# Legacy landing shell

This directory is deprecated.

- Canonical landing source: `apps/landing`
- Canonical dashboard source: `apps/dashboard`
- Canonical production hosts: `https://sardis.sh` and `https://app.sardis.sh`

Keep changes here minimal and reversible. The legacy Vite app exists only as a compatibility shell that redirects users to the canonical Next.js surfaces.
