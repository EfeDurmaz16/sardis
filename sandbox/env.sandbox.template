# Sardis Sandbox Environment Configuration
# Copy to .env or source directly: source sandbox/env.sandbox.template

# Environment
SARDIS_ENVIRONMENT=sandbox

# Security (sandbox-only key)
SARDIS_SECRET_KEY=sandbox-secret-key-not-for-production-use

# Database (local SQLite)
DATABASE_URL=sqlite:///./sandbox/data/sardis.db

# Chain execution (live on Base Sepolia for MVP demo)
# Set to "simulated" to avoid on-chain calls.
SARDIS_CHAIN_MODE=live

# Base Sepolia RPC (Alchemy/Ankr/etc.)
SARDIS_CHAINS__0__NAME=base_sepolia
SARDIS_CHAINS__0__RPC_URL=https://sepolia.base.org
SARDIS_CHAINS__0__CHAIN_ID=84532
SARDIS_CHAINS__0__STABLECOINS=USDC

# Local EOA signer for MVP (no MPC). REQUIRED for live mode.
# Example: export a fresh funded testnet key (never reuse in prod).
SARDIS_EOA_PRIVATE_KEY=put_testnet_private_key_here
SARDIS_EOA_ADDRESS=0xYourTestnetEOA

# MPC provider mode (keep local for MVP)
SARDIS_MPC__NAME=local

# API
SARDIS_API_BASE_URL=http://localhost:8001

# CORS (allow all for development)
SARDIS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173,http://localhost:8001

# Caching (in-memory)
REDIS_URL=

# Logging
SARDIS_LOG_LEVEL=DEBUG
SARDIS_LOG_FORMAT=pretty

# Demo API Key (for sandbox testing)
SARDIS_DEMO_API_KEY=sk_sandbox_demo_1234567890abcdef







