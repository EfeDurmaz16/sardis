{
  "installCommand": "npm install",
  "buildCommand": "npx --yes vite build",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/api/:path*", "destination": "https://api.sardis.sh/api/:path*" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
