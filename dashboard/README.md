# Legacy dashboard shell

This directory is deprecated.

- Canonical dashboard source: `apps/dashboard`
- Canonical landing source: `apps/landing`
- Canonical production host: `https://app.sardis.sh`

Keep changes here minimal and reversible. The legacy Vite dashboard exists only as a compatibility shell that redirects users to the canonical Next.js dashboard.
