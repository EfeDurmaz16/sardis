[project]
name = "sardis-mpp"
version = "0.1.0"
description = "Sardis MPP (Machine Payments Protocol) client — policy-governed HTTP 402 payments for AI agents"
requires-python = ">=3.11"
dependencies = ["httpx>=0.27", "pympp>=0.4.0"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
