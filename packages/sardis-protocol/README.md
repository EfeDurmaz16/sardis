# sardis-protocol

Adapters that make Sardis mandates and signatures AP2/TAP/x402 compliant. Includes:
- canonical JSON-LD/VC schemas for Intent/Cart/Payment
- TAP request binding utilities (domain, nonce, timestamp, purpose)
- mandate verification service + replay cache hooks
- deterministic audit hashing / transcript building
