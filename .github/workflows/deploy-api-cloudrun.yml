name: Deploy API to Cloud Run

on:
  push:
    branches: [main]
    paths:
      - 'packages/sardis-api/**'
      - 'packages/sardis-core/**'
      - 'packages/sardis-chain/**'

permissions: read-all

env:
  SERVICE_NAME: sardis-api
  REGION: us-east1

jobs:
  deploy:
    name: Build & Deploy to Cloud Run
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write

    steps:
      - name: Check GCP credentials
        id: check-gcp
        env:
          GCP_WIP: ${{ secrets.GCP_WORKLOAD_IDENTITY_PROVIDER }}
        run: |
          if [ -z "$GCP_WIP" ]; then
            echo "::error::GCP_WORKLOAD_IDENTITY_PROVIDER secret is not configured. Previously this step silently skipped the deploy, which meant backend changes never shipped — the running revision stayed frozen at whatever was last deployed manually. Set the secret in repository settings (Settings → Secrets → Actions) to enable automated Cloud Run deploys. Until then, backend deploys must be run manually via scripts/deploy-cloudrun.sh."
            exit 1
          fi

      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6

      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@7c6bc770dae815cd3e89ee6cdf493a5fab2cc093 # v3.0.0
        with:
          workload_identity_provider: ${{ secrets.GCP_WORKLOAD_IDENTITY_PROVIDER }}
          service_account: ${{ secrets.GCP_SERVICE_ACCOUNT }}

      - name: Set up Cloud SDK
        uses: google-github-actions/setup-gcloud@e427ad8a34f8676edf47cf7d7925499adf3eb74f # v2

      - name: Build with Cloud Build
        env:
          GCP_PROJECT_ID: ${{ secrets.GCP_PROJECT_ID }}
        run: |
          gcloud builds submit \
            --project="$GCP_PROJECT_ID" \
            --config=cloudbuild.yaml \
            --substitutions=_SERVICE_NAME=${{ env.SERVICE_NAME }},_REGION=${{ env.REGION }}

      - name: Deploy to Cloud Run
        env:
          GCP_PROJECT_ID: ${{ secrets.GCP_PROJECT_ID }}
        run: |
          gcloud run deploy ${{ env.SERVICE_NAME }} \
            --project="$GCP_PROJECT_ID" \
            --region=${{ env.REGION }} \
            --image="gcr.io/$GCP_PROJECT_ID/${{ env.SERVICE_NAME }}:latest" \
            --platform=managed \
            --allow-unauthenticated \
            --port=8000

      - name: Verify deployment
        env:
          GCP_PROJECT_ID: ${{ secrets.GCP_PROJECT_ID }}
        run: |
          URL=$(gcloud run services describe ${{ env.SERVICE_NAME }} \
            --project="$GCP_PROJECT_ID" \
            --region=${{ env.REGION }} \
            --format='value(status.url)')
          curl -sf "${URL}/health" || exit 1
